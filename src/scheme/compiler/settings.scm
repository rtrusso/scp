;;
;; Compiler settings:
;;
;; These are represented as global variables, set by the command-line
;; argument parser and used elsewhere in the code.
;;

(define *emit-global-marks* #t)

(define *emit-entry* #t)

(define *expand-only* #f)
